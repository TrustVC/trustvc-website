# permissionless@0.3.7 declares optional peer ox@^0.11.3, but viem@2.55.1 installs
# ox@0.14.30 which falls outside that range. The conflict is resolved via
# package.json#overrides so legacy-peer-deps is no longer needed.
